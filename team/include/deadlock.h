#ifndef SUSCRIPCION_H
#define SUSCRIPCION_H

#include "inicializacion.h"
/*
typedef struct{
    uint32_t idTrainer;
    char* pokemon;
} t_cycleDeadlock;

t_list* cycleDeadLock;
void detectDeadlock();
bool isCandidateDeadlock(t_trainer*);

t_list* getCycleDeadlock(t_trainer* blockedTrainer, char* pokemonNeeded);
*/
#endif